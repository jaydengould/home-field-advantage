# Core data / stats
pandas
numpy
statsmodels
linearmodels

# Sport data sources
pybaseball
nba_api
nfl_data_py

# Viz
matplotlib
seaborn

# Config / notebooks
pyyaml
jupyter

# Paper rendering.
# NOTE: the real Quarto is a standalone CLI (install via `brew install quarto`
# or https://quarto.org), NOT this pip package. Kept here per project spec, but
# rendering relies on the system `quarto` binary, not pip.
quarto
